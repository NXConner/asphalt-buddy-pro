import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.overwatch.app',
  appName: 'overwatch-app',
  webDir: 'dist'
};

export default config;
